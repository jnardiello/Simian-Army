box: wercker/php
build:
    # The steps that will be executed on build
    steps:
      # A custom script step, name value is used in the UI
      # and the code value contains the command that get executed
      - script:
          name: setting up environment
          code: sudo apt-get install php-pear php5-dev php5-curl
      - script:
          name: setting up MongoClient
          code: sudo pecl install mongo
      - script:
          name: install dependencies
          code: composer install --no-interaction --prefer-source
      - script:
          name: run unit tests
          code: vendor/bin/phpunit
      # Add more steps here:
      #- script:
      #    name: run unit tests
      #    code: phpunit

